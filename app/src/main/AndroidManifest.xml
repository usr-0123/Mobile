<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projo" >
        <activity
            android:name=".ui.home.ReportActivity"
            android:exported="false" />
        <activity
            android:name=".forms.CreateGroupActivity"
            android:exported="false" />
        <activity
            android:name=".pages.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Projo.NoActionBar" />
        <activity
            android:name=".forms.CreatePostActivity"
            android:exported="false" />
        <activity
            android:name=".pages.GroupActivity"
            android:exported="false" />
        <activity
            android:name=".pages.ChatActivity"
            android:exported="false"
            android:theme="@style/Theme.Projo" />
        <activity
            android:name=".pages.SignInActivity"
            android:exported="true"
            android:theme="@style/Theme.Projo.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>